---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart jaeger-ingester
  ansible.builtin.systemd:
    name: "{{ jaeger_ingester_service_name }}"
    state: restarted
